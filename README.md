# Similar-Shows
 The columns named "name" and "description" from the dataset are combined and preprocessed. Used word2vec to convert the combined column into a vector. Used cosine similarity to calculate the top 5 similar TV shows.
